import { InstructorRoleTitlePipe } from './instructor-role-title.pipe';

describe('InstructorRoleTitlePipe', () => {
  it('create an instance', () => {
    const pipe: InstructorRoleTitlePipe = new InstructorRoleTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
