/**
 * Represents the different sections of the instructor help page.
 */
export enum Sections {
    students = 'students',
    courses = 'courses',
    sessions = 'sessions',
    questions = 'questions',
    body = 'body',
}
