import { NotificationStyleClassPipe } from './notification-style-class.pipe';

describe('NotificationStyleClassPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationStyleClassPipe();
    expect(pipe).toBeTruthy();
  });
});
