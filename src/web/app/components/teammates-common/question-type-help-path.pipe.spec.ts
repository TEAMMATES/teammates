import { QuestionTypeHelpPathPipe } from './question-type-help-path.pipe';

describe('QuestionTypeHelpPathPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionTypeHelpPathPipe();
    expect(pipe).toBeTruthy();
  });
});
