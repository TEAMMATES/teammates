import { QuestionTypeIdPipe } from './question-type-id.pipe';

describe('QuestionTypeIdPipePipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionTypeIdPipe();
    expect(pipe).toBeTruthy();
  });
});
