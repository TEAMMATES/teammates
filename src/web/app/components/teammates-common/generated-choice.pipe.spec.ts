import { GeneratedChoicePipe } from './generated-choice.pipe';

describe('GeneratedChoicePipe', () => {
  it('create an instance', () => {
    const pipe: GeneratedChoicePipe = new GeneratedChoicePipe();
    expect(pipe).toBeTruthy();
  });
});
