import { ResponseTimeSeriesChart } from './response-time-series-chart.model';

describe('ResponseTimeSeriesChart', () => {
  it('should create an instance', () => {
    expect(new ResponseTimeSeriesChart()).toBeTruthy();
  });
});
