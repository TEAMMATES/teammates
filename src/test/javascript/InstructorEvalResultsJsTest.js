
module('instructorEvalResults.js');


test('showReport(id)', function(){
    // N/A, uses elements in the page
    expect(0);
});


test('openChildWindow(link)', function(){
    // N/A, opens new window
    expect(0);
});
