<?xml version="1.0" encoding="UTF-8"?>  
<queue-entries>
  
<queue>  
  <name>default</name>  
  <rate>4/s</rate>  
</queue>
<queue>  
  <name>configure-and-prepare-email-queue</name>  
  <!-- Configuration allows for reminders for 
       1 evaluation/feedback session to be queued 
       for execution every second when the bucket is empty
  -->
  <rate>1/s</rate>
  <bucket-size>1</bucket-size>  
</queue>


<queue>  
  <name>admin-prepare-email-task-queue</name>  
  <rate>1/s</rate>
  <bucket-size>1</bucket-size>  
  <retry-parameters>
      <task-retry-limit>2</task-retry-limit>
  </retry-parameters>
</queue>
<queue>  
  <name>admin-send-email-queue</name>  
  <rate>20/s</rate>
  <bucket-size>40</bucket-size>  
  <retry-parameters>
      <task-retry-limit>5</task-retry-limit>
      <task-age-limit>1d</task-age-limit>
      <min-backoff-seconds>30</min-backoff-seconds>
      <max-backoff-seconds>300</max-backoff-seconds>
      <max-doublings>0</max-doublings>
  </retry-parameters>
</queue>


<queue>  
  <name>feedback-submission-adjust-queue</name>  
  <!-- Processes each feedback adjustment sequentially
  -->
  <rate>1/s</rate>
  <bucket-size>5</bucket-size>
  <max-concurrent-requests>1</max-concurrent-requests>
  <retry-parameters>
      <task-retry-limit>2</task-retry-limit>
  </retry-parameters>  
</queue>     
<queue>  
  <name>search-document</name>  
  <rate>4/s</rate>  
</queue>  

<queue>
  <name>feedback-remind-email-queue</name>
   <!-- Configuration allows for 5 feedback reminder emails to be queued
       when the bucket is not full
  -->
  <rate>5/s</rate>
  <bucket-size>5</bucket-size> 
  <retry-parameters>
      <task-retry-limit>2</task-retry-limit>
  </retry-parameters>
</queue>
<queue>
  <name>feedback-remind-email-particular-users-queue</name>
   <!-- Configuration allows for 5 feedback reminder to particular 
       users emails to be queued when the bucket is not full
  -->
  <rate>5/s</rate>
  <bucket-size>5</bucket-size> 
  <retry-parameters>
      <task-retry-limit>2</task-retry-limit>
  </retry-parameters>
</queue>

<queue>  
  <name>send-email-queue</name>  
  <!-- Configuration allows for 20 emails to be queued
       when the bucket is not full
  -->
  <rate>10/s</rate>
  <bucket-size>20</bucket-size> 
  <retry-parameters>
      <task-retry-limit>5</task-retry-limit>
	  <task-age-limit>1d</task-age-limit>
	  <min-backoff-seconds>30</min-backoff-seconds>
      <max-backoff-seconds>300</max-backoff-seconds>
	  <max-doublings>0</max-doublings>
  </retry-parameters>  
</queue>   
</queue-entries> 
