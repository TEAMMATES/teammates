QUnit.module('instructorCourseDetails.js');

QUnit.test('toggleSendRegistrationKey(courseID, email)', (assert) => {
    assert.expect(0);
});

QUnit.test('toggleSendRegistrationKeysConfirmation(courseID)', (assert) => {
    // gives a popup, can't be tested
    assert.expect(0);
});
