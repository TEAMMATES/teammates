<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd
                                       http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="v9.0.0-beta.8-remove-invalid-read-notification-entries" author="Florian Viorel Tanely">
        <sql>
            DELETE FROM read_notifications
            WHERE account_id IS NULL OR notification_id IS NULL;
        </sql>
    </changeSet>

    <changeSet id="v9.0.0-beta.8-remove-duplicate-read-notification-entries" author="Florian Viorel Tanely">
        <sql>
            WITH numbered AS (
                SELECT id,
                       ROW_NUMBER() OVER (
                           PARTITION BY account_id, notification_id
                           ORDER BY created_at, id
                       ) as duplicate_rank
                FROM read_notifications
            )
            DELETE FROM read_notifications
            WHERE id IN (SELECT id FROM numbered WHERE duplicate_rank > 1);
        </sql>
    </changeSet>

    <changeSet id="v9.0.0-beta.8-add-unique-constraint-to-read-notifications" author="Florian Viorel Tanely">
        <addUniqueConstraint
                tableName="read_notifications"
                columnNames="account_id, notification_id"
                constraintName="Unique account_id and notification_id" />
        <addNotNullConstraint tableName="read_notifications" columnName="account_id" />
        <addNotNullConstraint tableName="read_notifications" columnName="notification_id" />
    </changeSet>

</databaseChangeLog>
