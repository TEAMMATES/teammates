{
  "name": "teammates-dev-docs",
  "private": true,
  "scripts": {
    "build": "markbind build",
    "serve": "markbind serve",
    "deploy": "markbind deploy --ci"
  },
  "devDependencies": {
    "markbind-cli": "^6.1.0"
  }
}
